import React from 'react';

const WorkspaceContext = React.createContext();

// eslint disable line below can be removed if/when we have more than a single context
export { WorkspaceContext }; // eslint-disable-line import/prefer-default-export
